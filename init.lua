-- A majority of this config was taken from
-- https://github.com/mikebarkmin/.dotfiles/tree/main/nvim/.config/nvim
-- both in the plugins used and the format.

require('plugins')
require('configs')
require('keybindings')
